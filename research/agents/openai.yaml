interface:
  display_name: "Research"
  short_description: "追一手来源，产出可复核笔记和可选 HTML 报告"
  default_prompt: "Use $research to investigate this question against primary sources, write a citable note, and generate an explanatory HTML report only when I explicitly request HTML or a web report."
